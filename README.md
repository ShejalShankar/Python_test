# Python Assessment

## The goal of this assessment is to display a square on the screen based on the assignment definition.

### Technical Constraints:

Strict adherence to the print() function for all outputs.

Input validation for invalid sizes (x≤0 or y≤0) with error messaging to stderr.

Clean repository management, excluding all __pycache__ and temporary files